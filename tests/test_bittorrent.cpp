#include <gtest/gtest.h>

/**
 * @file test_bittorrent.cpp
 * @brief General BitTorrent module tests
 * 
 * This file contains high-level tests that span multiple components.
 * Component-specific tests are in their respective test_bt_*.cpp files.
 */

// Basic sanity test
TEST(BitTorrentModuleTest, ModuleLoads) {
    // This test just verifies the module compiles and links correctly
    EXPECT_TRUE(true);
}
